apiVersion: v1
kind: Service
metadata:
  name: goapp
  namespace: demo-ops
  labels:
    app: goapp
spec:
  type: NodePort
  ports:
  - port: 6379
    protocol: TCP
    targetPort: goappport
  selector:
    name: goapp